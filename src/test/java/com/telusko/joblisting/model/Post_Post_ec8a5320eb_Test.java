// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting.model;

import org.junit.jupiter.api.Test;
import org.springframework.data.mongodb.core.mapping.Document;
import static org.junit.jupiter.api.Assertions.assertEquals;

public class Post_Post_ec8a5320eb_Test {

    @Test
    public void testPostConstructor() {
        Post post = new Post();
        assertEquals(Post.class, post.getClass());
    }

    @Test
    public void testPostConstructorWithParameters() {
        Post post = new Post("1", "Test Title", "Test Description", "Test Company", "Test Location");
        assertEquals("1", post.getId());
        assertEquals("Test Title", post.getTitle());
        assertEquals("Test Description", post.getDescription());
        assertEquals("Test Company", post.getCompany());
        assertEquals("Test Location", post.getLocation());
    }

}
