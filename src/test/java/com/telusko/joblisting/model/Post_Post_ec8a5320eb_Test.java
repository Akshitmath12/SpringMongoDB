// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting.model;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import static org.junit.jupiter.api.Assertions.*;

@SpringBootTest
public class Post_Post_ec8a5320eb_Test {

    @Test
    public void testPostConstructor() {
        Post post = new Post();
        assertNull(post.getProfile());
        assertNull(post.getDesc());
        assertEquals(0, post.getExp());
        assertNull(post.getTechs());
    }

    @Test
    public void testPostSettersAndGetters() {
        Post post = new Post();
        post.setProfile("Java Developer");
        post.setDesc("Java developer job description");
        post.setExp(1);
        post.setTechs(new String[]{"Java", "Spring Boot"});

        assertEquals("Java Developer", post.getProfile());
        assertEquals("Java developer job description", post.getDesc());
        assertEquals(1, post.getExp());
        assertEquals(Arrays.toString(new String[]{"Java", "Spring Boot"}), Arrays.toString(post.getTechs()));
    }
}
