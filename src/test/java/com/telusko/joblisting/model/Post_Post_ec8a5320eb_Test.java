// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting.model;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import static org.junit.jupiter.api.Assertions.*;

@SpringBootTest
public class Post_Post_ec8a5320eb_Test {

    @Test
    public void testPostConstructor() {
        Post post = new Post();
        assertNotNull(post, "Post object is null after initialization");
    }

    @Test
    public void testPostSettersAndGetters() {
        Post post = new Post();
        String profile = "Profile";
        String desc = "Description";
        int exp = 5;
        String[] techs = {"Java", "Spring Boot"};

        post.setProfile(profile);
        post.setDesc(desc);
        post.setExp(exp);
        post.setTechs(techs);

        assertEquals(profile, post.getProfile(), "Profile not set correctly");
        assertEquals(desc, post.getDesc(), "Description not set correctly");
        assertEquals(exp, post.getExp(), "Experience not set correctly");
        assertArrayEquals(techs, post.getTechs(), "Technologies not set correctly");
    }

    @Test
    public void testPostToString() {
        Post post = new Post();
        String profile = "Profile";
        String desc = "Description";
        int exp = 5;
        String[] techs = {"Java", "Spring Boot"};

        post.setProfile(profile);
        post.setDesc(desc);
        post.setExp(exp);
        post.setTechs(techs);

        String expectedString = "Post{" + "profile='" + profile + '\'' + ", desc='" + desc + '\'' + ", exp=" + exp + ", techs=" + Arrays.toString(techs) + '}';
        assertEquals(expectedString, post.toString(), "ToString method not working correctly");
    }
}
