// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting.model;

import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;
import static org.junit.Assert.*;

@RunWith(SpringRunner.class)
@SpringBootTest
public class Post_Post_ec8a5320eb_Test {
    
    private Post post;

    @Before
    public void setUp() {
        post = new Post();
    }

    @Test
    public void testPostConstructor() {
        assertNotNull("Post object should not be null", post);
    }

    @Test
    public void testPostAttributes() {
        assertNull("Post ID should be null", post.getId());
        assertNull("Post title should be null", post.getTitle());
        assertNull("Post description should be null", post.getDescription());
        assertNull("Post company should be null", post.getCompany());
        assertNull("Post location should be null", post.getLocation());
    }
}
