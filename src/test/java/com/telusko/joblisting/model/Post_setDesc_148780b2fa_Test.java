// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting.model;

import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class Post_setDesc_148780b2fa_Test {
    private Post post;

    @Before
    public void setup() {
        post = new Post();
    }

    @Test
    public void testSetDesc_validDescription() {
        String validDesc = "This is a valid description";
        post.setDesc(validDesc);
        Assert.assertEquals("Description should match", validDesc, post.getDesc());
    }

    @Test
    public void testSetDesc_emptyDescription() {
        String emptyDesc = "";
        post.setDesc(emptyDesc);
        Assert.assertEquals("Description should be empty", emptyDesc, post.getDesc());
    }
}
