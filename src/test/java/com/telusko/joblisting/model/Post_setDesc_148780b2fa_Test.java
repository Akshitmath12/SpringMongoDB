// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting.model;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;
import org.springframework.boot.test.context.SpringBootTest;

import static org.junit.jupiter.api.Assertions.assertEquals;

@SpringBootTest
public class Post_setDesc_148780b2fa_Test {

    private Post post;

    @BeforeEach
    public void setUp() {
        post = Mockito.mock(Post.class);
    }

    @Test
    public void testSetDesc_Success() {
        String expectedDesc = "This is a test description";
        Mockito.doAnswer(invocation -> {
            String desc = invocation.getArgument(0);
            assertEquals(expectedDesc, desc);
            return null;
        }).when(post).setDesc(expectedDesc);
        post.setDesc(expectedDesc);
    }

    @Test
    public void testSetDesc_Failure() {
        String wrongDesc = "This is a wrong test description";
        String expectedDesc = "This is a test description";
        Mockito.doAnswer(invocation -> {
            String desc = invocation.getArgument(0);
            assertEquals(expectedDesc, desc);
            return null;
        }).when(post).setDesc(wrongDesc);
        post.setDesc(wrongDesc);
    }
}
