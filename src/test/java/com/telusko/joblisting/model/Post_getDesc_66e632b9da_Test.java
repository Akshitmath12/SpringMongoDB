// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting.model;

import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;
import org.mockito.Mockito;

public class Post_getDesc_66e632b9da_Test {

    private Post post;

    @Before
    public void setUp() {
        post = Mockito.mock(Post.class);
    }

    @Test
    public void testGetDesc_Success() {
        String expectedDesc = "This is a test description";
        Mockito.when(post.getDesc()).thenReturn(expectedDesc);
        String actualDesc = post.getDesc();
        Assert.assertEquals(expectedDesc, actualDesc);
    }

    @Test
    public void testGetDesc_Null() {
        Mockito.when(post.getDesc()).thenReturn(null);
        String actualDesc = post.getDesc();
        Assert.assertNull(actualDesc);
    }
}
