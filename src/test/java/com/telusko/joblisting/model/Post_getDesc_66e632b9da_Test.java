// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting.model;

import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.mockito.MockitoAnnotations;
import org.springframework.data.mongodb.core.mapping.Document;

public class Post_getDesc_66e632b9da_Test {

    @Mock
    private Post post;

    @Before
    public void setUp() {
        MockitoAnnotations.initMocks(this);
    }

    @Test
    public void testGetDesc_Success() {
        Mockito.when(post.getDesc()).thenReturn("Test description");
        String result = post.getDesc();
        Assert.assertEquals("Test description", result);
    }

    @Test
    public void testGetDesc_Null() {
        Mockito.when(post.getDesc()).thenReturn(null);
        String result = post.getDesc();
        Assert.assertNull(result);
    }
}
