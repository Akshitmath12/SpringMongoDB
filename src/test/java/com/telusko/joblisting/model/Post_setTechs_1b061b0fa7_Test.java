// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting.model;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;
import static org.junit.jupiter.api.Assertions.assertArrayEquals;

public class Post_setTechs_1b061b0fa7_Test {

    private Post post;

    @BeforeEach
    public void setUp() {
        post = Mockito.mock(Post.class);
    }

    @Test
    public void testSetTechs_Success() {
        String[] expectedTechs = {"Java", "Spring Boot", "MongoDB"};
        Mockito.doAnswer(invocation -> {
            Object[] args = invocation.getArguments();
            assertArrayEquals(expectedTechs, (String[]) args[0]);
            return null;
        }).when(post).setTechs(Mockito.any(String[].class));
        post.setTechs(expectedTechs);
    }

    @Test
    public void testSetTechs_EmptyArray() {
        String[] expectedTechs = {};
        Mockito.doAnswer(invocation -> {
            Object[] args = invocation.getArguments();
            assertArrayEquals(expectedTechs, (String[]) args[0]);
            return null;
        }).when(post).setTechs(Mockito.any(String[].class));
        post.setTechs(expectedTechs);
    }
}
