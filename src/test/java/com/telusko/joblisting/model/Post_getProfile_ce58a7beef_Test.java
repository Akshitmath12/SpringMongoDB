// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting.model;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;

public class Post_getProfile_ce58a7beef_Test {

    @Test
    public void testGetProfile() {
        // Create a new Post object
        Post post = new Post();
        
        // Test when profile is null
        post.setProfile(null);
        assertEquals(null, post.getProfile(), "Failed to get profile when profile is null");
        
        // Test when profile is an empty string
        post.setProfile("");
        assertEquals("", post.getProfile(), "Failed to get profile when profile is an empty string");
        
        // Test when profile is a non-empty string
        post.setProfile("Software Engineer");
        assertEquals("Software Engineer", post.getProfile(), "Failed to get profile when profile is a non-empty string");
    }
}
