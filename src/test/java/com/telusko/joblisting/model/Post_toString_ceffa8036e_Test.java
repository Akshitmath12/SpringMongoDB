// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting.model;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;
import org.junit.runner.RunWith;
import static org.junit.jupiter.api.Assertions.assertEquals;

@RunWith(SpringRunner.class)
@SpringBootTest
public class Post_toString_ceffa8036e_Test {

    @Test
    public void testToStringMethodWithValidValues() {
        Post post = new Post();
        post.setProfile("Developer");
        post.setDesc("Java Developer");
        post.setExp(5);
        post.setTechs(new String[]{"Java", "Spring", "Hibernate"});

        String expected = "Post{profile='Developer', desc='Java Developer', exp=5, techs=[Java, Spring, Hibernate]}";
        String actual = post.toString();

        assertEquals(expected, actual);
    }

    @Test
    public void testToStringMethodWithNullValues() {
        Post post = new Post();
        post.setProfile(null);
        post.setDesc(null);
        post.setExp(0);
        post.setTechs(null);

        String expected = "Post{profile='null', desc='null', exp=0, techs=null}";
        String actual = post.toString();

        assertEquals(expected, actual);
    }
}
