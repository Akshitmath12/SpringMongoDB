// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting.model;

import org.junit.Test;
import static org.junit.Assert.assertEquals;

public class Post_toString_ceffa8036e_Test {

    @Test
    public void testToString() {
        // TODO: Set the values for profile, desc, exp and techs
        String profile = "Java Developer";
        String desc = "Looking for experienced Java Developer";
        int exp = 5;
        String[] techs = {"Java", "Spring Boot", "Microservices"};

        Post post = new Post();
        post.setProfile(profile);
        post.setDesc(desc);
        post.setExp(exp);
        post.setTechs(techs);

        String expectedResult = "Post{" +
                "profile='" + profile + '\'' +
                ", desc='" + desc + '\'' +
                ", exp=" + exp +
                ", techs=" + Arrays.toString(techs) +
                '}';

        assertEquals(expectedResult, post.toString());
    }

    @Test
    public void testToStringWithNullValues() {
        String profile = null;
        String desc = null;
        int exp = 0;
        String[] techs = null;

        Post post = new Post();
        post.setProfile(profile);
        post.setDesc(desc);
        post.setExp(exp);
        post.setTechs(techs);

        String expectedResult = "Post{" +
                "profile='" + profile + '\'' +
                ", desc='" + desc + '\'' +
                ", exp=" + exp +
                ", techs=" + Arrays.toString(techs) +
                '}';

        assertEquals(expectedResult, post.toString());
    }
}
