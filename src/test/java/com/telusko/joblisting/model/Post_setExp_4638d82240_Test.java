/*
Test generated by RoostGPT for test Java_test using AI Type Vertex AI and AI Model code-bison-32k

 **Test Scenario 1: Positive Integer Value**

**Objective**: Validate that the `setExp` method correctly sets the `exp` field with a positive integer value.

**Test Steps**:

1. Create a new instance of the class with the default constructor.
2. Call the `setExp` method with a positive integer value, such as 10.
3. Assert that the `exp` field of the object is equal to the value passed to the `setExp` method.

**Expected Result**: The `exp` field should be set to the positive integer value passed to the `setExp` method.

**Test Scenario 2: Negative Integer Value**

**Objective**: Validate that the `setExp` method correctly sets the `exp` field with a negative integer value.

**Test Steps**:

1. Create a new instance of the class with the default constructor.
2. Call the `setExp` method with a negative integer value, such as -10.
3. Assert that the `exp` field of the object is equal to the value passed to the `setExp` method.

**Expected Result**: The `exp` field should be set to the negative integer value passed to the `setExp` method.

**Test Scenario 3: Zero Value**

**Objective**: Validate that the `setExp` method correctly sets the `exp` field with a zero value.

**Test Steps**:

1. Create a new instance of the class with the default constructor.
2. Call the `setExp` method with a zero value, such as 0.
3. Assert that the `exp` field of the object is equal to the value passed to the `setExp` method.

**Expected Result**: The `exp` field should be set to the zero value passed to the `setExp` method.

**Test Scenario 4: Null Value**

**Objective**: Validate that the `setExp` method throws a `NullPointerException` when passed a null value.

**Test Steps**:

1. Create a new instance of the class with the default constructor.
2. Call the `setExp` method with a null value.
3. Assert that a `NullPointerException` is thrown.

**Expected Result**: A `NullPointerException` should be thrown when the `setExp` method is passed a null value.

**Test Scenario 5: Maximum Integer Value**

**Objective**: Validate that the `setExp` method correctly sets the `exp` field with the maximum integer value.

**Test Steps**:

1. Create a new instance of the class with the default constructor.
2. Call the `setExp` method with the maximum integer value, such as `Integer.MAX_VALUE`.
3. Assert that the `exp` field of the object is equal to the maximum integer value.

**Expected Result**: The `exp` field should be set to the maximum integer value passed to the `setExp` method.

**Test Scenario 6: Minimum Integer Value**

**Objective**: Validate that the `setExp` method correctly sets the `exp` field with the minimum integer value.

**Test Steps**:

1. Create a new instance of the class with the default constructor.
2. Call the `setExp` method with the minimum integer value, such as `Integer.MIN_VALUE`.
3. Assert that the `exp` field of the object is equal to the minimum integer value.

**Expected Result**: The `exp` field should be set to the minimum integer value passed to the `setExp` method.

**Test Scenario 7: Boundary Values**

**Objective**: Validate that the `setExp` method correctly sets the `exp` field with boundary values, such as 1, -1, and 0.

**Test Steps**:

1. Create a new instance of the class with the default constructor.
2. Call the `setExp` method with boundary values, such as 1, -1, and 0.
3. Assert that the `exp` field of the object is equal to the boundary values passed to the `setExp` method.

**Expected Result**: The `exp` field should be set to the boundary values passed to the `setExp` method.
*/
package com.telusko.joblisting.model;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.ValueSource;

import static org.junit.jupiter.api.Assertions.*;

class Post_setExp_4638d82240_Test {

    @DisplayName("Test setting experience with different values")
    @ParameterizedTest
    @ValueSource(ints = {10, -10, 0, Integer.MAX_VALUE, Integer.MIN_VALUE, 1, -1})
    void setExp_VariousValues(int exp) {
        // Create a new instance of the Post class
        Post post = new Post();

        // Call the setExp method with the specified value
        post.setExp(exp);

        // Assert that the exp field of the object is equal to the value passed to the setExp method
        assertEquals(exp, post.getExp());
    }

    @DisplayName("Test setting experience with a null value")
    @ParameterizedTest
    @ValueSource(ints = {0})
    void setExp_NullValue(int exp) {
        // Create a new instance of the Post class
        Post post = new Post();

        // Assert that a NullPointerException is thrown when the setExp method is passed a null value
        assertThrows(NullPointerException.class, () -> post.setExp(exp));
    }
}
