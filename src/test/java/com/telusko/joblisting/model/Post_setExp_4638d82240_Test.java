// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting.model;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.test.context.junit4.SpringRunner;
import static org.junit.Assert.*;

@RunWith(SpringRunner.class)
public class Post_setExp_4638d82240_Test {

    @Test
    public void testSetExp_Positive() {
        Post post = new Post();
        int expectedExp = 5;
        post.setExp(expectedExp);
        assertEquals("Failure - expected experience did not match", expectedExp, post.getExp());
    }

    @Test
    public void testSetExp_Negative() {
        Post post = new Post();
        int expectedExp = -1;
        try {
            post.setExp(expectedExp);
            fail("setExp method should have thrown an exception");
        } catch (IllegalArgumentException e) {
            assertEquals("Experience cannot be negative", e.getMessage());
        }
    }
}
