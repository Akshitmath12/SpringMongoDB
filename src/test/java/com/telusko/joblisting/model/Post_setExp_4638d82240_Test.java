// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting.model;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.util.Assert;

@SpringBootTest
public class Post_setExp_4638d82240_Test {

    @Autowired
    private Post post;

    @Test
    public void testSetExp_positive() {
        int expectedExp = 5;
        post.setExp(expectedExp);
        Assert.isTrue(post.getExp() == expectedExp, "setExp method has failed the test");
    }

    @Test
    public void testSetExp_negative() {
        int expectedExp = -3;
        post.setExp(expectedExp);
        Assert.isTrue(post.getExp() == expectedExp, "setExp method has failed the test");
    }

    @Test
    public void testSetExp_zero() {
        int expectedExp = 0;
        post.setExp(expectedExp);
        Assert.isTrue(post.getExp() == expectedExp, "setExp method has failed the test");
    }
}
