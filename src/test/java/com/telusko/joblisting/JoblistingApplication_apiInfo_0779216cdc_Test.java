// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;
import org.springframework.context.ApplicationContext;
import springfox.documentation.service.ApiInfo;
import springfox.documentation.builders.ApiInfoBuilder;

public class JoblistingApplication_apiInfo_0779216cdc_Test {
    
    @Test
    public void testApiInfo() {
        ApiInfoBuilder builderMock = Mockito.mock(ApiInfoBuilder.class);
        Mockito.when(builderMock.build()).thenReturn(new ApiInfo("title", "description", "version", "termsOfServiceUrl", "contact", "license", "licenseUrl", new ArrayList<>()));
        
        ApplicationContext context = Mockito.mock(ApplicationContext.class);
        Mockito.when(context.getBean(ApiInfoBuilder.class)).thenReturn(builderMock);

        JoblistingApplication application = new JoblistingApplication(context);
        ApiInfo apiInfo = application.apiInfo();
        
        Assertions.assertNotNull(apiInfo);
        Assertions.assertEquals("title", apiInfo.getTitle());
        Assertions.assertEquals("description", apiInfo.getDescription());
    }

    @Test
    public void testApiInfoNull() {
        ApiInfoBuilder builderMock = Mockito.mock(ApiInfoBuilder.class);
        Mockito.when(builderMock.build()).thenReturn(null);
        
        ApplicationContext context = Mockito.mock(ApplicationContext.class);
        Mockito.when(context.getBean(ApiInfoBuilder.class)).thenReturn(builderMock);

        JoblistingApplication application = new JoblistingApplication(context);
        ApiInfo apiInfo = application.apiInfo();
        
        Assertions.assertNull(apiInfo);
    }
}
