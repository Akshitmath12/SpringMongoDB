/*
Test generated by RoostGPT for test Java_test using AI Type Vertex AI and AI Model code-bison-32k

 **Test Scenario 1: Validate the presence of ApiInfo**

**Test Objective:** To verify that the `apiInfo()` method returns an instance of `ApiInfo`.

**Test Steps:**

1. Create a JUnit test class for the `SwaggerConfig` class.
2. Write a test method to invoke the `apiInfo()` method and assert that the returned value is not null.

**Expected Result:**

The test should pass, indicating that the `apiInfo()` method returns an instance of `ApiInfo`.

**Test Scenario 2: Validate the ApiInfo details**

**Test Objective:** To verify that the `ApiInfo` object returned by the `apiInfo()` method contains the expected details, such as title, description, version, etc.

**Test Steps:**

1. Create a JUnit test class for the `SwaggerConfig` class.
2. Write a test method to invoke the `apiInfo()` method and assert that the returned `ApiInfo` object contains the expected details.
3. Use the `assertEquals()` method to compare the actual values with the expected values.

**Expected Result:**

The test should pass, indicating that the `apiInfo()` method returns an `ApiInfo` object with the expected details.

**Test Scenario 3: Validate the ApiInfo metadata**

**Test Objective:** To verify that the `ApiInfo` object returned by the `apiInfo()` method contains the expected metadata, such as contact information, license, terms of service, etc.

**Test Steps:**

1. Create a JUnit test class for the `SwaggerConfig` class.
2. Write a test method to invoke the `apiInfo()` method and assert that the returned `ApiInfo` object contains the expected metadata.
3. Use the `assertEquals()` method to compare the actual values with the expected values.

**Expected Result:**

The test should pass, indicating that the `apiInfo()` method returns an `ApiInfo` object with the expected metadata.
*/
import com.telusko.joblisting.SwaggerConfig;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import springfox.documentation.service.ApiInfo;

import static org.junit.jupiter.api.Assertions.*;

@SpringBootTest
public class JoblistingApplication_apiInfo_0779216cdc_Test {

    @Test
    public void testApiInfo_NotNull() {
        SwaggerConfig swaggerConfig = new SwaggerConfig();
        ApiInfo apiInfo = swaggerConfig.apiInfo();

        assertNotNull(apiInfo, "ApiInfo should not be null");
    }

    @Test
    public void testApiInfo_Details() {
        SwaggerConfig swaggerConfig = new SwaggerConfig();
        ApiInfo apiInfo = swaggerConfig.apiInfo();

        // TODO: Set expected details for title, description, version, etc.
        String expectedTitle = "Job Listing API";
        String expectedDescription = "This API provides endpoints for managing job listings.";
        String expectedVersion = "1.0.0";

        assertEquals(expectedTitle, apiInfo.getTitle(), "Title should match");
        assertEquals(expectedDescription, apiInfo.getDescription(), "Description should match");
        assertEquals(expectedVersion, apiInfo.getVersion(), "Version should match");
    }

    @Test
    public void testApiInfo_Metadata() {
        SwaggerConfig swaggerConfig = new SwaggerConfig();
        ApiInfo apiInfo = swaggerConfig.apiInfo();

        // TODO: Set expected metadata for contact, license, terms of service, etc.
        String expectedContactName = "John Doe";
        String expectedContactEmail = "john.doe@example.com";
        String expectedLicense = "Apache License 2.0";
        String expectedTermsOfService = "https://example.com/terms-of-service";

        assertEquals(expectedContactName, apiInfo.getContact().getName(), "Contact name should match");
        assertEquals(expectedContactEmail, apiInfo.getContact().getEmail(), "Contact email should match");
        assertEquals(expectedLicense, apiInfo.getLicense(), "License should match");
        assertEquals(expectedTermsOfService, apiInfo.getTermsOfServiceUrl(), "Terms of service should match");
    }
}
