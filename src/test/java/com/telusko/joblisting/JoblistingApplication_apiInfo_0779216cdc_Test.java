// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;
import springfox.documentation.service.ApiInfo;

import static org.junit.Assert.assertNotNull;

@RunWith(SpringRunner.class)
@SpringBootTest
public class JoblistingApplication_apiInfo_0779216cdc_Test {

    private JoblistingApplication joblistingApplication = new JoblistingApplication();

    @Test
    public void testApiInfo() {
        ApiInfo apiInfo = joblistingApplication.apiInfo();
        assertNotNull("ApiInfo should not be null", apiInfo);
    }

    @Test
    public void testApiInfoProperties() {
        ApiInfo apiInfo = joblistingApplication.apiInfo();
        // TODO: here we should test the properties of the ApiInfo object,
        // for example, if we know that the title should be "API Documentation",
        // we could add:
        // assertEquals("API Documentation", apiInfo.getTitle());
    }
}
