// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import springfox.documentation.service.ApiInfo;
import static org.junit.jupiter.api.Assertions.*;

@SpringBootTest
public class JoblistingApplication_apiInfo_0779216cdc_Test {

    @Test
    public void testApiInfo() {
        ApiInfo apiInfo = new JoblistingApplication().apiInfo();
        assertNotNull(apiInfo);
        assertEquals("", apiInfo.getTitle());
        assertEquals("", apiInfo.getDescription());
        assertEquals("", apiInfo.getVersion());
        assertEquals("", apiInfo.getTermsOfServiceUrl());
        assertNull(apiInfo.getContact());
        assertNull(apiInfo.getLicense());
        assertNull(apiInfo.getLicenseUrl());
    }

    @Test
    public void testApiInfoWithBuilder() {
        ApiInfo apiInfo = new JoblistingApplication().apiInfo();
        apiInfo = new ApiInfo(
                "Title", "Description", "Version",
                "TermsOfServiceUrl", apiInfo.getContact(),
                "License", "LicenseUrl", apiInfo.getVendorExtensions());

        assertEquals("Title", apiInfo.getTitle());
        assertEquals("Description", apiInfo.getDescription());
        assertEquals("Version", apiInfo.getVersion());
        assertEquals("TermsOfServiceUrl", apiInfo.getTermsOfServiceUrl());
        assertEquals("License", apiInfo.getLicense());
        assertEquals("LicenseUrl", apiInfo.getLicenseUrl());
    }
}
