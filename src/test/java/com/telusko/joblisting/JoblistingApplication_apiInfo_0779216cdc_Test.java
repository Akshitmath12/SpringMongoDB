// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.BeforeEach;
import org.mockito.InjectMocks;
import org.mockito.MockitoAnnotations;
import org.springframework.boot.test.context.SpringBootTest;
import springfox.documentation.service.ApiInfo;
import springfox.documentation.builders.ApiInfoBuilder;

import static org.junit.jupiter.api.Assertions.*;

@SpringBootTest
public class JoblistingApplication_apiInfo_0779216cdc_Test {

    @InjectMocks
    private JoblistingApplication joblistingApplication;

    @BeforeEach
    public void init() {
        MockitoAnnotations.initMocks(this);
    }

    @Test
    public void testApiInfo() {
        ApiInfo apiInfo = joblistingApplication.apiInfo();
        assertNotNull(apiInfo);
        assertNull(apiInfo.getDescription());
        assertNull(apiInfo.getTitle());
        assertNull(apiInfo.getVersion());
        assertNull(apiInfo.getTermsOfServiceUrl());
        assertNull(apiInfo.getLicense());
        assertNull(apiInfo.getLicenseUrl());
        assertNull(apiInfo.getContact());
    }

    @Test
    public void testApiInfoWithBuilder() {
        ApiInfoBuilder builder = new ApiInfoBuilder();
        builder.title("Test Title");
        builder.description("Test Description");
        builder.version("Test Version");
        ApiInfo apiInfo = builder.build();

        assertNotNull(apiInfo);
        assertEquals("Test Title", apiInfo.getTitle());
        assertEquals("Test Description", apiInfo.getDescription());
        assertEquals("Test Version", apiInfo.getVersion());
    }
}
