// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting.controller;

import org.junit.Before;
import org.junit.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;
import org.springframework.mock.web.MockHttpServletResponse;

import static org.junit.Assert.assertEquals;
import static org.mockito.Mockito.verify;

public class PostController_redirect_31b271143a_Test {

    @InjectMocks
    PostController postController;

    @Mock
    MockHttpServletResponse response;

    @Before
    public void init() {
        MockitoAnnotations.initMocks(this);
    }

    @Test
    public void testRedirect() throws IOException {
        postController.redirect(response);
        verify(response).sendRedirect("/swagger-ui.html");
    }

    @Test
    public void testRedirectFailure() throws IOException {
        try {
            postController.redirect(response);
            verify(response).sendRedirect("/invalid-url");
        } catch (AssertionError e) {
            assertEquals("Error: Redirection to wrong URL", e.getMessage());
        }
    }
}
