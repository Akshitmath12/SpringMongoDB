// Test generated by RoostGPT for test JavaUnitTest using AI Type Open AI and AI Model gpt-4

package com.telusko.joblisting;

import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.mockito.Mockito.when;
import static org.mockito.Mockito.mock;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import springfox.documentation.spring.web.plugins.Docket;
import springfox.documentation.spi.DocumentationType;
import springfox.documentation.builders.RequestHandlerSelectors;
import springfox.documentation.builders.PathSelectors;
import springfox.documentation.service.ApiInfo;

@ExtendWith(MockitoExtension.class)
public class JoblistingApplication_api_24e94226d8_Test {

    @InjectMocks
    private JoblistingApplication joblistingApplication;

    @Mock
    private DocumentationType documentationType;

    @Mock
    private RequestHandlerSelectors requestHandlerSelectors;

    @Mock
    private PathSelectors pathSelectors;

    @Mock
    private ApiInfo apiInfo;

    @BeforeEach
    public void setUp() {
        when(documentationType.SWAGGER_2).thenReturn(mock(DocumentationType.class));
        when(requestHandlerSelectors.withClassAnnotation(RestController.class)).thenReturn(mock(RequestHandlerSelectors.class));
        when(pathSelectors.any()).thenReturn(mock(PathSelectors.class));
        when(joblistingApplication.apiInfo()).thenReturn(mock(ApiInfo.class));
    }

    @Test
    public void testApi() {
        Docket docket = joblistingApplication.api();
        assertNotNull(docket);
    }

    @Test
    public void testApiInfo() {
        ApiInfo apiInfo = joblistingApplication.apiInfo();
        assertNotNull(apiInfo);
    }
}
